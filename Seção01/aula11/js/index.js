alert('Com a nossa mensagem. ') // é um atalho para window.alert

window.confirm('Deseja realmente apagar? ') // com o window.confirm
//usamos para confirmar algo vai retornar true ou false

const nome = window.prompt('Digite seu nome: ') // com o window.prompt
// usamos para que o usuario digite algo, retorna o valor digitado ou null se cancelar

window.alert(nome)