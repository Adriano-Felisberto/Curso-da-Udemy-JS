// atividida

//const ePaisagem = (largura, altura) => {
//    return largura > altura ? true : false;
//}
//

const ePaisagem = (largura, altura) => {
    return largura > altura;
}

const modo = ePaisagem(300, 200)

console.log(modo);
